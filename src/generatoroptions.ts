

/**
 * Base options common to all generators
 */
export interface GeneratorOptions
{
  /**
   * If true, console output will be colored, else it will not
   */
  color?:boolean,
  /**
   * If true, console output will be silenced
   */
  quiet?:boolean,
}